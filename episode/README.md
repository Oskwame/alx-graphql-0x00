GraphQL Queries

Episode by ID

- Use `episode-page-1.graphql` and provide variables like `{ "id": "1" }`.

Characters by Page

- Run `characters-page-2.graphql`, `characters-page-3.graphql`, and `characters-page-4.graphql` to fetch character `id` and `name` for pages 2–4.

Saving Outputs

- Paste results into `characters-page-1-output.json`, `characters-page-2-output.json`, `characters-page-3-output.json`, and `characters-page-4-output.json` using the GraphQL response shape `{ "data": { ... } }`.
